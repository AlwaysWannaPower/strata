[package]
name = "strata-app"
version.workspace = true
edition.workspace = true
description = "Desktop UI (Dioxus) of the strata workbench."

[dependencies]
strata-core = { path = "../strata-core" }
dioxus = { version = "0.7", features = ["desktop"] }
rfd = "0.17"
